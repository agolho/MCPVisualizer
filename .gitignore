# Dependencies
node_modules/

# Build outputs
dist/
build/
*.tsbuildinfo
.cache/

# Electron packaging output (big binaries — uploaded to GitHub Releases, not git)
packages/app/release/
packages/app/out/

# Ingester scratch + outputs
packages/ingester/.work/
packages/ingester/out/
packages/viz/public/graph.json

# Local project data (dev fallback when running without Electron)
.mcpviz/
.mcpviz-dev/

# Env + secrets
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editors / OS
.DS_Store
Thumbs.db
.idea/
*.swp
*.swo

# Claude Code workspace — keep shared launch.json, ignore per-user settings
.claude/settings.local.json
.claude/cache/
